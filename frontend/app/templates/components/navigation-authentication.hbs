<ul class="nav navbar-nav navbar-right">
  {{#unless session.isAuthenticated}}
    <li>{{#link-to 'login'}}Login{{/link-to}}</li>
  {{else}}
    {{#if session.isAuthenticated}}
      <li><a>{{session.currentUser.name}}</a></li>
    {{/if}}
    <li>
      <a href="#" {{action 'logout'}}>Logout</a>
    </li>
  {{/unless}}
</ul>
